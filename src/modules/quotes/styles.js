import CustomStyleSheet from '../../utils/CustomStyleSheet';

export const styles = CustomStyleSheet({
  alertContainer: {
    position: 'absolute',
    top: 100,
    zIndex: 1000,
    alignSelf: 'center'
  }
});
